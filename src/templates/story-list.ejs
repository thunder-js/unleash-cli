import React from 'react';
import { storiesOf } from '@storybook/react-native';
import <%= componentName %> from './'

const mockFetchActions = {
  refetch: () => null,
  fetchMore: () => null,
}
const mockFetchState = {
  activelyRefetching: false,
}

const <%= arrayName %> = <%- JSON.stringify(fakeDataArray, null, 2) %>

storiesOf('<%= componentName %> Component', module)
  .add('default', () => (
    <<%= componentName %>
      <%= arrayName %>={<%= arrayName %>}
      onPressItem={() => {}}
      fetchActions={mockFetchActions}
      fetchState={mockFetchState}
    />
  ))


/*
      <%_ for (var i = 0; i < fakeData.length; i++) { -%>
      <%_ var prop = fakeData[i], key = prop.key, value = prop.value, type = prop.type -%>
      <%_ if (type === 'number') {-%>
      <%=key%>={<%= value -%>}
      <%_} -%>
      <%_ if (type === 'multiline-string') {-%>
      <%=key%>={`<%= value -%>`}
      <%_} -%>
      <%_ if (type === 'string') {-%>
      <%=key%>='<%= value -%>'
      <%_} -%>
      <%_} -%>
      */