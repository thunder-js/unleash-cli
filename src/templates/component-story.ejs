import React from 'react';
import { storiesOf } from '@storybook/react-native';
import <%= name %> from './';

storiesOf('<%= name %> Component', module)
  .add('default', () => (
    <<%= name %> />
  ))